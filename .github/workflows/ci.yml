name: CI WORKFOLW
on:
  push:
jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      -name: checkout code
       uses: actions/checkout@v4

      name: setup docker
      uses: docker/setup-buildx-action@v4
